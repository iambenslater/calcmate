<%- include('partials/head') %>
<% if (article.faq && article.faq.length) { %>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    <% article.faq.forEach((f, i) => { %>
    {
      "@type": "Question",
      "name": "<%- (f.q || f.question || '').replace(/"/g, '\\"') %>",
      "acceptedAnswer": { "@type": "Answer", "text": "<%- (f.a || f.answer || '').replace(/"/g, '\\"') %>" }
    }<%= i < article.faq.length - 1 ? ',' : '' %>
    <% }) %>
  ]
}
</script>
<% } %>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": "<%- (article.title || '').replace(/"/g, '\\"') %>",
  "description": "<%- (article.metaDescription || article.excerpt || '').replace(/"/g, '\\"') %>",
  "url": "<%- siteUrl %>/articles/<%- article.slug %>",
  "image": "<%- siteUrl %>/images/og-default.png",
  "datePublished": "<%- article.datePublished || '2026-04-12' %>",
  "dateModified": "<%- article.dateModified || article.datePublished || '2026-04-12' %>",
  "publisher": { "@type": "Organization", "name": "CalculatorMate", "url": "<%- siteUrl %>" },
  "author": { "@type": "Person", "name": "Ben Slater", "url": "<%- siteUrl %>/about" }
}
</script>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Home", "item": "<%- siteUrl %>" },
    { "@type": "ListItem", "position": 2, "name": "Articles", "item": "<%- siteUrl %>/articles" },
    { "@type": "ListItem", "position": 3, "name": "<%- (article.title || '').replace(/\x22/g, '\\\"') %>", "item": "<%- siteUrl %>/articles/<%- article.slug %>" }
  ]
}
</script>
<%- include('partials/ad-hero-banner') %>
<%- include('partials/header') %>

<main class="flex-1 max-w-7xl mx-auto w-full px-4 sm:px-6 lg:px-8 py-6">
  <!-- Breadcrumbs -->
  <nav aria-label="Breadcrumb" class="mb-4">
    <ol class="flex items-center gap-2 text-sm text-gray-500">
      <li><a href="/" class="hover:text-navy transition">Home</a></li>
      <li class="text-gray-300">/</li>
      <li><a href="/articles" class="hover:text-navy transition">Articles</a></li>
      <li class="text-gray-300">/</li>
      <li class="text-gray-800 font-medium"><%= article.title %></li>
    </ol>
  </nav>

  <div class="lg:flex gap-8">
    <!-- Main content -->
    <div class="flex-1 min-w-0">
      <h1 class="text-3xl font-bold text-navy mb-3"><%= article.title %></h1>
      <p class="text-gray-500 text-sm mb-4"><%= article.datePublished || '12 April 2026' %> · <%= article.readTime || '5' %> min read</p>

      <!-- Article content -->
      <article class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 md:p-8 mb-8">
        <div class="article-content">
          <%- article.content %>
        </div>
      </article>

      <!-- Editorial pick after article -->
      <%- include('partials/ad-editorial-pick') %>

      <!-- Inline Amazon CTA -->
      <%- include('partials/affiliate-inline') %>

      <!-- Related calculators -->
      <% if (relatedCalcs && relatedCalcs.length) { %>
        <div class="mb-8">
          <h2 class="text-xl font-bold text-navy mb-4">Try These Calculators</h2>
          <div class="grid sm:grid-cols-2 gap-3">
            <% relatedCalcs.forEach(c => { %>
              <a href="/<%= c.category %>/<%= c.slug %>" class="group bg-white rounded-lg p-4 shadow-sm hover:shadow-md border border-gray-100 hover:border-gold/50 transition">
                <h3 class="font-medium text-navy group-hover:text-gold transition text-sm"><%= c.title %></h3>
                <p class="text-xs text-gray-500 mt-1"><%= c.description %></p>
                <span class="text-xs text-gold font-medium mt-2 inline-block">Try it free →</span>
              </a>
            <% }) %>
          </div>
        </div>
      <% } %>

      <!-- FAQ -->
      <% if (article.faq && article.faq.length) { %>
        <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-8">
          <h2 class="text-xl font-bold text-navy mb-4">Frequently Asked Questions</h2>
          <div class="space-y-4">
            <% article.faq.forEach((f, fIdx) => { %>
              <details class="group border-b border-gray-100 pb-3">
                <summary class="cursor-pointer font-medium hover:text-gold transition flex justify-between gap-2 text-sm sm:text-base leading-snug py-1" style="color: #00205B;">
                  <span class="flex-1" style="color: #00205B;"><%= f.q || f.question || '' %></span>
                  <svg class="w-5 h-5 shrink-0 mt-0.5" style="color: #9ca3af;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                </summary>
                <p class="mt-2 text-sm leading-relaxed" style="color: #4b5563;"><%= f.a || f.answer || '' %></p>
              </details>
            <% }) %>
          </div>
        </div>
      <% } %>
    </div>

    <!-- Sidebar -->
    <aside class="w-full lg:w-72 mt-8 lg:mt-0 shrink-0">
      <div class="sticky top-20 space-y-4">
        <!-- Share -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-4">
          <%- include('partials/share', { calc: { category: 'articles', slug: article.slug, title: article.title, description: article.metaDescription || article.excerpt || '' } }) %>
        </div>

        <!-- Amazon product cards -->
        <%- include('partials/ad-product') %>

        <!-- More articles -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-4">
          <h3 class="font-semibold text-navy mb-3 text-sm uppercase tracking-wider">More Articles</h3>
          <nav class="space-y-1">
            <% articles.filter(a => a.slug !== article.slug).slice(0, 8).forEach(a => { %>
              <a href="/articles/<%= a.slug %>" class="block px-3 py-1.5 rounded-lg text-xs hover:bg-gray-50 text-gray-600 transition"><%= a.title %></a>
            <% }) %>
            <a href="/articles" class="block px-3 py-1.5 rounded-lg text-xs text-gold font-medium hover:bg-gold-light transition">View all articles →</a>
          </nav>
        </div>
      </div>
    </aside>
  </div>
</main>

<%- include('partials/ad-footer-banner') %>
<%- include('partials/footer') %>
<%- include('partials/tail') %>
